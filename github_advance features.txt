1. Runner types
- github hosted
- 3rd party hosted
- self-hosted

2. persisting data ( artifacts )

by default, jobs are run in ephemeral environments ! 
Artifacts provide a mechanism to persist data beyone the lifecycle of a job.

syntax
to upload artifact 
uses: actions/upload-artifact@4

to consume the artifact
uses: actions/download-artifact@v4

3. persisting data ( cache )
official: actions/cache@v4
data stored in remote object store
capacity by default 10GB]

3. namespace cache volumes.

4. Github token permission

contents: read
packages: read

etc.

5. External service auth.

6. Matrix and conditionals.
- concurrency 
- matrix
- if


